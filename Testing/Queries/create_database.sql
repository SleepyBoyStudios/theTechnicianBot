
VACUUM;

CREATE TABLE User_Info (
    user_ID INTEGER(20) NOT NULL PRIMARY KEY ,
    total_XP INTEGER(20),
    level INTEGER(20),
    time INTEGER(20),
    is_restricted BOOLEAN
);

CREATE TABLE Server_991178883682541700 (
    server_991178883682541700_rec_ID INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    user_ID INTEGER(20) NOT NULL,
    server_xp INTEGER(20),
    FOREIGN KEY (user_ID) REFERENCES User_Info(user_ID)
);

CREATE TABLE Server_810002138830471178 (
    server_810002138830471178_rec_ID INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    user_ID BIGINT NOT NULL,
    server_xp INTEGER(20),
    FOREIGN KEY (user_ID) REFERENCES User_Info(user_ID)
);